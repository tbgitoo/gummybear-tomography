"""High-level validation and plotting helpers for the gummybear forward model.

This companion package holds milestone notebook checks, diagnostic plots, and
small assertion helpers. It does not implement physics or dataset generation;
it wraps ``gummybear`` outputs for human review and pytest contracts.
"""

version = "0.0.1.dev0"
__version__ = version
